use crate::FlagArgument;

/// Generates the flag usage
pub(super) fn generate<Options>(
    header: Option<&dyn std::fmt::Display>,
    flags: &[&dyn FlagArgument<Options>],
) {
    todo!()

    // Partition flags into groups

    // Display groups
}
